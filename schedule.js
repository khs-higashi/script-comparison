// JavaScript Document
// カレンダー作成用の期間指定
document.addEventListener('DOMContentLoaded', function () {
    // スケジュールページの要素チェック
    if (!document.getElementById('start-year-select')) {
        // 必要な要素がない場合は処理を中断
        console.log('Schedule elements not found, script execution skipped');
        return;
    }
    
    // 以下、元のコードをそのまま続ける...
    const yearStart = 2025;
    // ...
});